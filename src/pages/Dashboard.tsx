import React from "react";
import { Typography, Container } from '@mui/material';

export const Dashboard:React.FC = () => {

    return (<Container><Typography variant="h4">Dashboard</Typography></Container>);

}